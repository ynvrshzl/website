# Data structures
Kit containing core data structures for computer science and algorithms.

Methods:

- [Object](#object)
- [Stack](#stack)
- [Queue](#queue)

## .object()
Key value pair. A simpler object without curly braces or strange syntax. 

## .stack()
Last in, first out

## .queue()
Implements the "queue" data structure from computer science. Items first in, are last out.

## .graph()

## .linkedlist()

## .matrix()
This method creates a matrix (a grid like chess) useful for tables that require spreadsheet data.

On a more technical definition... creates table rows from an matrix of arrays. The matrix can be either an array of arrays... or returned from a .map()

<!-- 
## Concept
The matrix is a series of rows, columns and cells. Useful for spreadsheets or tables.

```yaml
[row][col][col][col]
[row][itm][itm][itm]
[row][itm][itm][itm]
[row][itm][itm][itm]
``` 

## Examples
Example usage of this method...
```js
const data = [
    ["1", "2", "3"],
    ["1", "2", "3"],
    ["1", "2", "3"],
];
template("table").matrix(data);
```

-->


## .tree()
From an array list of items, outputs a structured list that can used inside an HTML list.

<!-- Here is an example of how to use this method:

```javascript
const list = [1, 2, 3, "string"];
template("list").tree(list);
``` -->

