# Templates
Web development is time consuming. And re-writing the same layouts, html, logic, wiring can get incredibly time consuming. 

The idea of templates shine in this area.

## .table()
Tables are painful to work with in PLAIN HTML. That's why I created this.

## .section()
## .container()
## .wrapper()
## .spacer('200px')

## .grid()
Creates a CSS Grid with areas. The grid takes a matrix table and displays based on defined areas.

## .header()

## .resizer()
Convert two containers into rezisable spaces by attaching a drag handle inbetween them.

```js
select("div").resizer()
```

## .blueprint()
Create your own reuseable components and layouts