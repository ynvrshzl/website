## Loops
The reason loops are so difficult to wok with is becuase they are three dimensional structures, and we human beings can only do one thing at a time. By flattening the structure...

```c++
loop(3).for(3).agent(i).when(agent).lessthan(3).not(5).sudo(...)
```