## Functions
Instead of raw functions, we can treat a function like a flow... 

```js
fx("my new function").does(action => action).click();
```

To use the function we just created...

```js
fx("my new function")
```