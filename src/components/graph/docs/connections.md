# Connections between nodes
The NetWork Graph visualizes connections between links, based on the current [Mode](./api/modes.md)

- __"Local"__ mode visualizes connections between nodes, based on the root node, which is the local node, so it's logically connected to the local nodes.
- __"Global"__ mode visualizes connections between nodes, based on the [Database property: "outlinks" and "inlinks"]