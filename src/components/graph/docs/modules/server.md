# Server()
The "server" is the continous loop that runs every browser frame. This can be thought of as a factory conveyor belt. The belt carries packages along the factory. Each package is unique in it's purpose... they can carry data, information, instructions and more.

An example:
- Browser frame 0
- Browser frame 1