## The Fundamental Model
Canvas = Source, Cartesian Axis = Interoperable layer.

The canvas becomes the Source, and the Cartesian Axis is an interpoerable layer, between Canvas coordinates, and Human-readable graphs. This is basically the model we're working with here. Everything else is just programmable translations to make these systems work!

# Conceptual Models
Conceptual psychomodels for this System to make sense.

- __Canvas__ - The source units, of the Canvas HTML. E.g. (Width: 300, Height: 500)
- __Median__ - Is a calculated value from half of the Canvas. Essentially, dividing it's x and y axis into a single subdivision, resulting in 4 axis with sequential units. E.g. (150, 250)
- __Quadrant__ - Each Quadrant Contains 2wo axis from which values are calculated from the above units. In total, there are 4 Quadrants, each requiring...?
- __Cartesian Planes__ - 