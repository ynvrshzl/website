# Modes
Modes are ways to set visualization data sources. Of the Network Graph. They change how the Graph sources data and visualizes it.

- **"local"** the graph will visualize the current site active article as the main source 
- **"global"** the graph will visualize connections from *all articles on site
- **"source"** the graph will visualize it's connections from an object as a data source 