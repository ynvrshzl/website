# Templates
Templates allows frontend to have it's layout dynamically shift based on it's "template" 

## How it works
Usually, this module itself handles deciding conditions for each article and how to apply a template. 

For example, a file located in "wiki/world/..." has the default world template applied to it. 